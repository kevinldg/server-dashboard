name: Deployment
on: [push]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Install node packages
        run: npm i
      - name: Build the repository
        run: npm run dev
      - name: Start the web app in dev environment
        run: npm run start
