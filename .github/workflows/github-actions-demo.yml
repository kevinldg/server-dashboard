name: Deployment
on: [push]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Install node packages
        run: npm i
      - name: Build the repository
        run: npm run build
      - name: Start the web app
        run: npm run start
